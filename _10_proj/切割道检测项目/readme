思路一：
1、根据图像特点，将图像X方向投影为一个"像素"，Y方向保持不变。
2、在在Y方向上计算梯度，构建多个特征向量，构成特征库（特征包含梯度信息、离边缘的距离等）。
具体为: a、每个方向取五个检测点（RGB通道），将五个监测点的梯度、坐标构成特征向量（坐标的中心移动至原点）
        b、采集多个图像收集若干个特征向量保存为特征库。
        C、
3、利用knn计算近邻，区分哪个像素是芯片有效区域的边缘。


a.每个方向取5个点

思路二：



